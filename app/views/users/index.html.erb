<h1>Users</h1>

<table>
    <tbody>
        <% @users.each do |user| %>
            <tr>
                <td><%= user.name %></td>
                <td><%= link_to "Edit", edit_user_path(path) %></td>
            </tr>
        <% end %>
    </tbody>
</table>

<%= link_to "New User", new_user_path %>
